alias: "Notify if the right garage door has been open too long"
trigger:
  - platform: state
    entity_id: cover.garage_right 
    to: 'open'
    for:
      hours: 2
action:
  - service: notify.pushover
    data:
      message: "Right garage door is still open"
      title: "Garage door"
